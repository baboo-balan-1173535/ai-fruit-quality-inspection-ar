# Kiwi Sorter — Detection Platform (Flask, port 5000)
# Install:  python -m venv venv && venv\Scripts\pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2
python-dotenv==1.2.2

# ── Computer vision / detection ──────────────────────────────
ultralytics==8.4.35          # YOLOv8 object detection
opencv-python==4.13.0.92     # frame capture, GrabCut, drawing
numpy==2.4.4
pillow==12.2.0

# ── AI quality analysis ──────────────────────────────────────
anthropic==0.91.0            # Claude Vision (claude-opus-4-5)

# ── Database ──────────────────────────────────────────────────
psycopg2-binary==2.9.12      # PostgreSQL driver (scan_reports)

# ── TLS for the phone HTTPS listener (live camera, port 5443) ──
cryptography>=42

# torch / torchvision are pulled in by ultralytics automatically.
